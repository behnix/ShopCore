@page
@model App.Pages.Admin.ConfigGroup.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Admin.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfigGroup[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigGroup[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigGroup[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigGroup[0].CategoryId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConfigGroup) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
